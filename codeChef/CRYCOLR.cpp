#include <bits/stdc++.h>
using namespace std;
void solve(){
    int max;
    int arr[9];

    cin >> max;
    for(int i=0; i<9; i++)
        cin >> arr[i];

    for(int i=0; i<9; i++)
        cin >> arr[i];
        
}
int main(){
    int t;
    cin >> t;
    while(t--)
        solve();
}